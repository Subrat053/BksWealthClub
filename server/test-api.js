import axios from "axios";
import dotenv from "dotenv";

dotenv.config();

// We need an admin token to call this
// Since I don't have one, I'll just check the controller logic by mocking the aggregate results.
// Actually, I can just log the results inside the controller.
